/**
 * @author KCG
 * @since July 13, 2017
 */

define([], function () {
	var fn = {
		init: function() {
            this.initElements();
            this.bindEvents();
            this.render();
        },

        bindEvents: function() {
        },

        render: function() {
        } 
	};

	return fn;
});